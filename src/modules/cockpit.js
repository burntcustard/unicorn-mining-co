// A ship cannot fly without somewhere to sit. Its health comes from the hull
// segment it is built into, because it is cut to fit each ship
export const cockpit = {
  critical: true,
  name: 'Cockpit',
  price: 500,
};
