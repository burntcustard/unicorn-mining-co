import { colors } from '../colors';

// Platinum
// An ingot, drawn as a bar with its lower edge drawn in a little so that it
// reads as a solid with a top face rather than a flat rectangle
export const platinum = {
  bounciness: 0.05,
  health: 70,
  name: 'Platinum',
  points: [[-8, -4], [8, -4], [6, 4], [-6, 4]],
  price: 1000,
  shades: colors.grey,
  shine: true,
};
